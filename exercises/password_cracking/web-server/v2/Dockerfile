FROM kalilinux/kali-rolling
EXPOSE 80 22

RUN apt-get update
CMD for user in $("Cem" "Nicole" "Bilal" "Maxim") do \
    && echo $user \
    && done

# RUN for user in $("Cem" "Nicole" "Bilal" "Maxim"); do \
# 	    useradd \$user; \
# 	    password=\$(shuf -n 1 /usr/share/wordlists/fasttrack.txt); \
#     done;

